# Tastytrade OAuth personal grant (create at my.tastytrade.com -> API)
# Grant only the READ scope. Never commit this file's real twin (.env).
TT_SECRET=your_client_secret_here
TT_REFRESH=your_refresh_token_here
# paper = cert/sandbox environment, live = production (still read-only)
TT_ENV=paper
PORT=8420
